# Alfred Scholar Brand Assets

Drop official brand exports here so every agent can reference them.

Expected files (add as available):

- `logo.svg`, `logo-dark.svg`: the `A` mark
- `wordmark.svg`: mark plus "Alfred Scholar" lockup
- `favicon.svg`
- `og-image.png`: social share card

Source of truth for live assets is the website (alfredscholar.com) and the product repo at `~/code/alfredscholar`. Recheck there before recreating the logo.
