# Austa Brand Assets

Drop official brand exports here so every agent can reference them.

Expected files (add as available):

- `favicon.svg`: the circular austa mark (live at austa.in/favicon.svg)
- `wordmark.svg`: lowercase "austa" lockup
- `og-image.png`: social share card (live at austa.in/og-image.png)

Design sources live in `~/code/austa/design`. Fonts: Fraunces, Instrument Serif, Geist Mono. Theme: light `#f7f9fd`, dark `#070b16`, vermillion accent.
