[core]
excludesfile = ~/.global_gitignore
editor = nvim
[user]
email = bhushangaykawad@gmail.com
name = Bhushan
[credential]
helper = cache
[pull]
ff = only
[init]
defaultBranch = main
[rerere]
	enabled = true
[push]
	autoSetupRemote = true
[gpg]
	format = ssh
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

# Delta configuration (Nord syntax theme, Catppuccin-inspired diff colors)
[core]
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta]
	navigate = true
	side-by-side = false
	line-numbers = true
	syntax-theme = Nord
	minus-style = syntax "#3e2a2e"
	minus-emph-style = syntax "#5e3a3e"
	plus-style = syntax "#1e3a2f"
	plus-emph-style = syntax "#2e5a3f"
	line-numbers-minus-style = "#f38ba8"
	line-numbers-plus-style = "#a6e3a1"
	line-numbers-zero-style = "#6c7086"
	hunk-header-style = syntax bold
	hunk-header-decoration-style = "#45475a" box
[merge]
	conflictstyle = diff3
[diff]
	colorMoved = default
